import { Typography } from "@/components/common/Typography";

const MyProvidentList = () => {
  return (
    <section id="my provident list">
      <Typography variant="topic" size="topic">
        My provident list
      </Typography>
      <div>My provident list Card</div>
    </section>
  );
};

export default MyProvidentList;
